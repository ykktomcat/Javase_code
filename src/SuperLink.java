//超级链表
public class SuperLink {
    //链表的头部
    private Node head;
    // 对数据的增删改查

    // 增加一个元素
    private void add(int data) {
        if (head == null) {
            head = new Node(data, null);
        }

    }


}

